---
const phoneNumber = import.meta.env.PHONE_NUMBER;
---
<section class="bg-dark text-light text-center text-uppercase">
	<div class="container py-5">
		<p class="fs-3 fw-bold">Call Now For A Quote</p>
		<a href={`tel:${phoneNumber}`} class="btn btn-danger btn-lg"><i class="bi bi-telephone-fill me-2"></i> {phoneNumber}</a>

	</div>
</section>