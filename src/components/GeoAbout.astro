---
const {city, aboutArea, subAreas} = Astro.props;
---
<article class="py-5">
    <div class="container">
        <div class="bg-light p-3 border">

            <h3 class="fw-bold">About {city}</h3>
            <p class="lead">{aboutArea}</p>
            <p class="lead">Our mobile tyre fitting service is available in many areas of {city}, such as: <span class="fw-bold">{subAreas}</span></p>
        </div>
    </div>
</article>